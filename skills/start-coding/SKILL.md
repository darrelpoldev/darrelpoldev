---
name: start-coding
description: Implement the requirements from a plan produced by /plan-with-me. Use whenever the user says "start coding" or "let's build this" — but ONLY if a plan file exists.
---

# Start Coding

- Precondition: a plan/spec/RFC from /plan-with-me MUST exist — no code without a plan
- If asked to code without one (even from a ticket), stop and suggest /plan-with-me first
- Confirm which plan file is being implemented before writing code
- Stay within the plan; park out-of-scope discoveries instead of chasing them
- Implement in small increments; verify as you go
- Once done coding, suggest /review-code
